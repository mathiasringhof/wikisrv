# Open Questions

- Should the sample include image attachments in addition to PDF assets?
- Should frontmatter validation eventually move into a dedicated lint command?
- Should the server expose directory indexes for raw assets?
